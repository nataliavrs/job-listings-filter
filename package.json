{
  "name": "joblistingsfiltering",
  "version": "1.0.0",
  "description": "Job Listings Filtering",
  "scripts": {
    "start": "parcel index.html",
    "serve": "json-server --watch db.json --port 3001",
    "build": "parcel build index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nataliavrs/job-listings-filter.git"
  },
  "author": "Natalia Veras",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.17.4",
    "parcel": "^2.10.2"
  }
}
